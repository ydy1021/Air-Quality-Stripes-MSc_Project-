import { IOBuffer } from 'iobuffer';
import { Header } from './header';
import { readType } from './types';
/**
 * Read data for the given non-record variable
 * @param buffer - Buffer for the file data
 * @param variable - Variable metadata
 * @return - Data of the element
 */
export declare function nonRecord(buffer: IOBuffer, variable: Header['variables'][number]): Array<ReturnType<typeof readType>>;
/**
 * Read data for the given record variable
 * @param buffer - Buffer for the file data
 * @param variable - Variable metadata
 * @param recordDimension - Record dimension metadata
 * @return - Data of the element
 */
export declare function record(buffer: IOBuffer, variable: Header['variables'][number], recordDimension: Header['recordDimension']): Array<ReturnType<typeof readType>>;
//# sourceMappingURL=data.d.ts.map